/*
 * @Copyright 2022. Institute for Future Intelligence, Inc.
 */

import React from 'react';

export interface HeatTransferSimulationProps {
  city: string | null;
}

const HeatTransferSimulation = ({ city }: HeatTransferSimulationProps) => {
  return <></>;
};

export default React.memo(HeatTransferSimulation);
